# Metadata Column Assignment

Renames metadata columns so they match the column names expected by PROTzilla, such as `Sample`, `Group`, or `Batch`. This is useful when the imported metadata file uses different column names.
